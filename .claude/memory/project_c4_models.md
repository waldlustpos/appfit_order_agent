---
name: project-c4-models
description: kiosk_v4 스타일 C4 모델을 appfit_order_agent·kokonut_order_agent_v2·did 3개 repo에 적용 완료 (2026-07-03)
metadata: 
  node_type: memory
  type: project
  originSessionId: 95cefa17-de2c-4518-b6c3-b4e83e7a0210
---

2026-07-03에 kiosk_v4의 `kioskc4model/` 손수 작성 정적 HTML C4 모델 방식을 3개 repo에 동일 적용하고 커밋했다 — appfit_order_agent는 `main`(c3157f6), kokonut은 `feature/kds-mode`(115c9c3), did는 `feat/mediaplayer-engine`(0f7d7f1). **kokonut·did는 feature 브랜치에 있으므로 main 반영은 해당 브랜치 머지에 따라감** (필요 시 cherry-pick). push는 안 함.

- `appfit_order_agent/agentc4model/` — 17페이지 (L1·L2·L3×7·L4×5·views×3)
- `kokonut_order_agent_v2/kokonutc4model/` — 15페이지 (L3×6·L4×4·views×3)
- `did/didc4model/` — 14페이지 (L3×5·L4×4·views×3)
- As-Is 문서: 각 repo `docs/AS-IS.md` + 규약 정본 `appfit_order_agent/docs/C4_GUIDE.md` (Outline 위키 게시용, GFM 전용)

규약: 공용 `c4core.css/js`·`views/c4.css`는 kiosk_v4 원본과 byte 동일(수정 금지), `views/c4.js`의 `VIEWS[]`만 프로젝트별. c4Graph의 from/to/label id 누락은 **조용히 skip**되므로 수정 시 id 대조 필수. 진입점은 각 폴더 `c4core-context.html`.

주의: 브리프/탐색 보고서의 `label_print_orchestrator.dart`는 kokonut에만 존재 — appfit_order_agent의 라벨 오케스트레이션 실체는 `lib/core/orders/output_service.dart`. kokonut의 `docs/ARCHITECTURE.md`는 "검증 필요" 상태라 테넌트 값은 `lib/config/server_config.dart`가 정본.

**2026-07-04 A(appfit) 검증 패스 완료** (main 6f51428, push 대기): 에이전트 fan-out으로 사실 주장 653건 코드 대조 → 66건 정정 + headless Chrome 스크린샷 시각 검증. **AS-IS.md 내용이 바뀌었으므로 Outline에 이미 게시했다면 재게시 필요** (appfit_core 의존·모니터링·출력 파이프라인 행 변경). **B(kokonut) 검증 패스 완료** (feature/kds-mode 6b23a19, push 대기): 9-에이전트 fan-out + headless Chrome 시각 검증으로 15페이지 정정, verify_c4.py를 kokonutc4model/에도 복사(자립 검사, 0오류). kokonut 고유 계통 오류(A의 7종과 다름): ① **Firestore write 주체 = K069(alessi) 단독** — server_config.dart 주석의 "K066·K071 서버측"은 stale, 런타임 정본은 `preference_service.getFirestoreDirectIntegration`(K069만 기본 OFF, 나머지 client write). ② **Dio baseUrl "갱신 단계"는 stale** — `dio_config.dart`의 `BrandBaseUrlInterceptor`가 매 요청 checkURL 주입(baked-in 아님·비면 reject), 유출 위험은 재연결 전 소켓·reset() 누락. ③ **output-print "3채널 공통 큐" 오모델** — 실제 외부영수증만 escpos_builder→printer_job_queue→Transport, Sunmi=MethodChannel 직결, 라벨=이미지+autoreplyprint 독립. ④ HTTPS→HTTP·`/api/msa/order/`는 미서명(wald_signer는 `/api/v2/*`). ⑤ WaldNDK/.so 제거(com/waldget/stamp/util=EncryptUtil·CommonUtil)·waldpos Windows전용·소켓포트 9049~9091. **AS-IS.md 변경 → Outline 재게시 필요.** **C(did) 검증 패스 완료** (feat/mediaplayer-engine 1bd599f, push 대기): 5-에이전트 fan-out + headless Chrome 시각 검증으로 14페이지 정정, verify_c4.py를 didc4model/에도 복사(0오류). did 고유 계통 오류: ① **비디오 엔진 3종**(MediaKit/ExoPlayer/MediaPlayer) — `_migrateOnUpdate`가 Android를 **MediaPlayer**로 1회성 강제 이관(문서의 "Android→ExoPlayer"는 오기 · main.dart:188; feat/mediaplayer-engine 브랜치 변경이 C4/AS-IS에 미반영된 게 원인). ② 폴백 캐시 클래스 **ContentsCacheService→ContentCacheHelper**(배너 JSON 캐시는 helper, ContentsCacheService는 미디어 프리로드). ③ **C:\video HALLUCINATION**(코드 부재 → /sdcard/video·앱 문서폴더/video). ④ Sentry **breadcrumb 전송 직전 전량 제거**(_StripBreadcrumbsProcessor, main.dart:317) — "브레드크럼 전송"은 정반대. ⑤ appfit_core ref v1.0.13→v1.0.15(AS-IS), 폴링 60/15s(10s 오기), app-shell 부팅순서 ⑤⑥·라우트(/content, splash 경유 redirect)·화면 8종. api-version-detector·appfit-vs-legacy·order-number-race는 정정 0(검증 통과). **3개 repo(A·B·C) C4 검증 전부 완료.** AS-IS.md 3종 **Outline 재게시 완료** (2026-07-05, waldlust.getoutline.com `documents.update`로 기존 문서 3개 본문 갱신 — appfit-as-is-HjTUCDQrKz / kokonut-as-is-HyVwWmA4Av / did-as-is-jOtAVolWL9, 제목·URL 유지). Outline 토큰은 `did/.env`의 `OUTLINE_KEY`. **3개 검증 커밋 원격 push 완료** (2026-07-05 origin 최신 확인: appfit main 6f51428 / kokonut feature/kds-mode 6b23a19 / did feat/mediaplayer-engine 1bd599f). kokonut·did는 feature 브랜치이므로 main 반영은 각 브랜치 머지 시점에 따라감. **프로젝트 완결** (C4 3repo 작성→검증→Outline 게시→커밋/push 전부 완료).

B/C 검증 시 반드시 확인할 A에서 발견된 계통 오류: ① 미병합 브랜치 기능을 main 사실처럼 기재(Slack 로그 업로드), ② 삭제된 클래스 잔존 표기(LabelPrintOrchestrator — kokonut에는 실존하니 B는 반대로 OK), ③ 제거된 Firestore 동기화(A만; B는 실사용), ④ 의존성 path vs git ref, ⑤ 폴링 고정 주기 표기(실제 적응형), ⑥ 메서드→클래스 귀속 오기, ⑦ 복구 큐(markPendingReprint)를 프린터 실패와 혼동. docs/AUTH_FLOW.md·ARCHITECTURE.md(LabelPrintOrchestrator 표기)도 stale — 후속 수정 후보.

**2026-07-06 4번째 repo = appfit_core(공유 라이브러리) C4 신규 작성·게시 완료** (appifit_agent_core repo, 브랜치 feature/remote-log-collection, 미커밋). 앞선 3개(앱)와 **성격이 달라 판형을 바꿈**: L1의 "사용자"는 사람이 아니라 **소비자 앱**(order_agent path·DID git ref·kiosk), **하드웨어 노드 없음**(라이브러리는 device I/O 미소유), L2 "컨테이너"는 런타임 프로세스가 아닌 **lib/src 논리 모듈 7클러스터**(Config·Auth&Crypto·HTTP·Socket&Events·Cache&Utils·Monitoring&Logging·OTA — 페이지에 "런타임 컨테이너 아님" 주석 명시), 가장 중요한 외부 계약 = **추상 인터페이스 3종**(AppFitLogger·AuthStateProvider·MonitoringContext). 폴더 `appifit_agent_core/corec4model/` 18페이지(L1·L2·L3×7·L4×6[token-manager·crypto-utils·dio-provider·notifier-service·socket-dispatcher·monitoring-service]·views×3[인증토큰흐름·소켓생명주기·이벤트분류]), 동결 자산 byte-identical 복사·views/c4.js VIEWS[]만 교체·verify_c4.py 0오류·headless Chrome 시각검증 통과. `docs/AS-IS.md`(라이브러리 판형) 신규 + CLAUDE.md 링크 + appfit_order_agent `docs/C4_GUIDE.md` 적용저장소 목록에 5번째로 추가. **Outline 게시 완료**(waldlust.getoutline.com, 기존 3개와 동일 컬렉션 6f453ef0…, documents.create → `appfit_core-as-is-0MNqg8q6lG`, id 755f239d, 제목 "appfit_core As-Is"). 주의: packageVersion 상수(1.0.14)와 pubspec(1.1.0) 불일치 — sync_version이 release.sh에서만 도는 정상 상태(AS-IS에 명기). 소스 사실: MonitoringService는 breadcrumb **전송**함(DID의 breadcrumb 제거 계통오류와 반대 — appfit_core는 전송이 정상).

**2026-07-06 전체 C4 모델(HTML) Outline 게시 + 재사용 도구화 완료.** As-Is 요약 4종은 이미 게시돼 있었고(부모 "에이전트/DID" 971e537f, 컬렉션 "SX팀" 6f453ef0), 이번에 **인터랙티브 HTML C4 모델 전체(64페이지)를 각 As-Is 문서 하위 "C4 아키텍처 모델" 자식 문서로 신규 게시**. Outline은 Markdown+이미지 위키라 HTML 직접 렌더 불가 → **레벨별 스크린샷 임베드 + 원본 c4model zip 첨부** 방식. 게시 문서: appfit=c4-5z5z2Eyd0O(17p·부모 fe02c5d8) / kokonut=c4-QBLglbESpE(15p·62b33451) / did=c4-CtCskrQny7(14p·0e488fd8) / appfit_core=c4-IaVwTNojzg(18p·755f239d). **재사용 스크립트 `<c4폴더>/publish_outline.py`**(자립 Python·stdlib만) 4개 repo에 커밋·push: appfit main 59f87b5 / kokonut feature/kds-mode 6516d6e / did feat/mediaplayer-engine aa9eba8 / core docs/c4-model 3c16859. 스크립트 핵심: ① **CDP `Page.captureScreenshot(captureBeyondViewport)`로 전체페이지 캡처**(창높이 고정 방식은 position:fixed 요소가 창 하단에 박혀 실패 — 반드시 CDP 전체높이), Chrome 1개 재사용(17회 콜드스타트→1회, 페이지당 1~2s), stdlib 최소 WebSocket 클라이언트 자체구현. ② Outline **attachments.create → S3 presigned multipart POST(204) → `![](/api/attachments.redirect?id=)` 임베드**(redirect는 302→서명 S3 URL, 브라우저가 인증헤더 없이 따라가 렌더; urllib로 직접 GET하면 auth 헤더 재전송돼 400 오탐). ③ 멱등: `.outline_c4_doc_id` sidecar(커밋됨)로 재실행 시 documents.update. **OUTLINE_KEY 배치**: 각 repo 루트 `.env`(gitignore)에 OUTLINE_KEY/URL/COLLECTION_ID/ASIS_DOC_ID. **appifit_agent_core는 .gitignore에 .env가 누락돼 있어 보강**(docs/c4-model 브랜치, 그 repo는 현재 docs/c4-model 체크아웃 상태로 둠 — feature/remote-log-collection의 .gitignore엔 .env 미포함이라 되돌리면 .env 노출 위험). did/.env에만 있던 키를 4개 repo로 확산. 재게시: 아키텍처 변경 후 `python3 <c4폴더>/publish_outline.py` 한 방.

아키텍처 변경 시 갱신 순서: 코드 → C4 뷰 → AS-IS.md → Outline 재게시(As-Is는 documents.update, C4 모델은 publish_outline.py 재실행). 관련: [[project-refactor-audit-2026-06]]
